# Models

Auty uses **DeepFace** with the **ArcFace** backend. Model weights are downloaded automatically on first use (cached under your user home directory by the framework).

No model files are stored in this repository.

To pre-build embeddings from photos, use `scripts/build_db.py` and `scripts/profile_sources/`.
